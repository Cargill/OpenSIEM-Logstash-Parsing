input {
  pipeline {
    address => VAR_PIPELINE_NAME
  }
}
filter {
  mutate {
    add_field => { 
      "event.module" => "Azure O365 DLP Logs" 
      }
  }

  
}
output {
  pipeline { send_to => [enrichments] }
}