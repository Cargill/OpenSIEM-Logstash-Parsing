input {
  pipeline { address => elastic_output }
}
output {
  elasticsearch {
    hosts => [VAR_ELASTIC_SERVERS]
    index => "VAR_ELASTIC_INDEX"
    ssl => true
    ssl_certificate_verification => false
    user => "VAR_ELASTIC_USER"
    password => "VAR_ELASTIC_PASSWORD"
    ilm_enabled => false
  }
}